@model IEnumerable<Outsourcing.Data.Models.Notification>
@using Outsourcing.Data.Models

@{
    ViewBag.Title = "Index";
}

@section scripts{
    <script src="/Content/admin/js/jquery.dataTables.min.js" type="text/javascript"></script>
    <script src="/Content/admin/js/jquery.dataTables.bootstrap.js" type="text/javascript"></script>
}

<div class="page-content">
    <div class="row">
        <div class="col-xs-12">
            @*<div class="alert alert-block alert-success">
                    <button type="button" class="close" data-dismiss="alert">
                        <i class="icon-remove"></i>
                    </button>
                    <i class="icon-ok green"></i>
                    Quản lý tin tức
                </div>*@
            @*<a class="btn btn-success" href="/Admin/Notification/Create" style="margin-bottom:10px">Thêm mới Cảnh báo</a>*@

            <div class="row" style="margin-bottom:10px">

            </div>


            <div class="table-header">Danh sách Cảnh báo</div>

            <table id="NotificationTable" class="table table-bordered table-striped table-fixed-header">
                <thead>
                    <tr role="row">
                        <th>STT</th>
                        <th>Cảnh báo</th>
                        <th>Kiểu Cảnh Báo</th>
                        <th>Quan Trọng</th>
                        <th>Đã xem</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody role="alert" aria-live="polite" aria-relevant="all">
                    @if (Model != null && Model.Any())
                    {
                        int count = 0;
                        foreach (var item in Model)
                        {
                            count++;
                            <tr class="odd">
                                <td class=" sorting_1">@count</td>
                                <td class=" ">@item.Name</td>
                                <td class=" ">@item.TypeNotify.Name</td>
                                <td class=" ">
                                    @if (item.isUrgent)
                                    {
                                        <span class="btn-danger">Quan trọng</span>
                                    }
                                    else
                                    {
                                        <span>Bình Thường</span>
                                    }
                                </td>
                                <td class=" ">
                                    @if (item.isRead)
                                    {
                                        <span class="btn-warning">Đã Đọc</span>
                                    }
                                    else
                                    {
                                        <span class="btn-info">Chưa đọc</span>
                                    }
                                </td>
                                <td class=" ">
                                    <div class="visible-md visible-lg hidden-sm hidden-xs btn-group">

                                        <a href="/Admin/Notification/Edit?NotificationId=@item.Id" class="btn btn-xs btn-info">
                                            <i class="icon-edit bigger-120"></i>
                                        </a>
                                        <a href="/Admin/Notification/Delete?NotificationId=@item.Id" class="btn btn-xs btn-danger">
                                            <i class="icon-trash bigger-160"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script type="text/javascript">



    $(document).ready(function () {
        var table = $("#NotificationTable").DataTable();
        //var arrayfilter = [2, 4];

        createDropFilter(table, [2]);
        bindDropFilter(table, [6]);

    });

    function createDropFilter(table, array) {
        array.forEach(function (i) {
            var selector = ".filter_" + i;
            var filter1 = $(selector);
            filter1.on('change', function () {
                table.column(i)
                    .search($(this).val())
                    .draw();
            });

            table.column(i).data().unique().sort().each(function (d, j) {
                filter1.append('<option value="' + d + '">' + d + '</option>');
            });

        });
    }
    function bindDropFilter(table, array) {
        array.forEach(function (i) {
            var selector = ".filter_" + i;
            var filter1 = $(selector);
            filter1.on('change', function () {
                table.column(i)
                    .search($(this).val())
                    .draw();
            });
        });
    }
</script>